---
import type { ImageMetadata } from 'astro';
import Media from '../Media.astro';
import SectionCta from './SectionCta.astro';

interface Column {
  heading: string;
  body: string;
  ratio: string;
  alt: string;
}

interface Props {
  eyebrow?: string;
  heading?: string;
  columns: Column[];
  images: (ImageMetadata | undefined)[];  ctaLabel?: string;
  ctaHref?: string;
}

const { eyebrow, heading, columns, images, ctaLabel, ctaHref } = Astro.props;
---

<section class="feature-grid">
  {
    (eyebrow || heading) && (
      <div class="feature-grid__intro">
        {eyebrow && <p class="feature-grid__eyebrow">{eyebrow}</p>}
        {heading && <h2 class="feature-grid__heading">{heading}</h2>}
      </div>
    )
  }
  <SectionCta label={ctaLabel} href={ctaHref} />
  <div class="feature-grid__columns">
    {
      columns.map((column, index) => (
        <div class="feature-grid__column">
          <Media fit="natural" image={images[index]} ratio={column.ratio} alt={column.alt} />
          <h3 class="feature-grid__column-heading">{column.heading}</h3>
          <p class="feature-grid__column-body">{column.body}</p>
        </div>
      ))
    }
  </div>
</section>

<style>
  .feature-grid {
    max-width: var(--project-width);
    margin: 0 auto;
    padding: var(--sp-6) var(--sp-3);
    display: flex;
    flex-direction: column;
    gap: var(--sp-5);
  }

  .feature-grid__intro {
    display: flex;
    flex-direction: column;
    gap: var(--sp-2);
  }

  .feature-grid__eyebrow {
    font-size: var(--font-size-meta);
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--color-muted);
  }

  .feature-grid__columns {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--sp-4);
  }

  .feature-grid__column {
    display: flex;
    flex-direction: column;
    gap: var(--sp-2);
  }

  .feature-grid__column-body {
    font-size: var(--font-size-body);
    line-height: 1.7;
    color: var(--color-muted);
    text-align: left;
  }

  @media (max-width: 767px) {
    .feature-grid__columns {
      grid-template-columns: 1fr;
    }
  }
</style>
