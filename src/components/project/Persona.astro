---
import type { ImageMetadata } from 'astro';
import Media from '../Media.astro';

interface PersonaCard {
  name: string;
  ratio: string;
  alt: string;
}

interface Props {
  eyebrow?: string;
  heading: string;
  personas: PersonaCard[];
  images: (ImageMetadata | undefined)[];
}

const { eyebrow, heading, personas, images } = Astro.props;
---

<section class="persona">
  <div class="persona__intro">
    {eyebrow && <p class="persona__eyebrow">{eyebrow}</p>}
    <h2 class="persona__heading">{heading}</h2>
  </div>
  <div class="persona__grid">
    {
      /*
        只放圖，不再把 persona 的資料用文字重列一次：那張圖本身就是完整的人物看板
        （姓名、角色、Goals、Needs、Personality 都印在圖上），旁邊再寫一次是重複的。
        內容裡的 name 不輸出到頁面，只用來讓後台版位標籤認得「這格是誰」。
      */
      personas.map((persona, index) => (
        <div class="persona__card">
          <Media fit="natural" image={images[index]} ratio={persona.ratio} alt={persona.alt} />
        </div>
      ))
    }
  </div>
</section>

<style>
  .persona {
    max-width: var(--project-width);
    margin: 0 auto;
    padding: var(--sp-6) var(--sp-3);
    display: flex;
    flex-direction: column;
    gap: var(--sp-5);
  }

  .persona__intro {
    display: flex;
    flex-direction: column;
    gap: var(--sp-2);
  }

  .persona__eyebrow {
    font-size: var(--font-size-meta);
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--color-muted);
  }

  .persona__grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: var(--sp-4);
  }
</style>
