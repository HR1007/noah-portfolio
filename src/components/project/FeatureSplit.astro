---
import type { ImageMetadata } from 'astro';
import Media from '../Media.astro';
import Button from '../Button.astro';

interface Props {
  heading: string;
  body: string;
  imagePosition: 'left' | 'right';
  image: ImageMetadata | undefined;
  ratio: string;
  alt: string;
  ctaLabel?: string;
  ctaHref?: string;
}

const { heading, body, imagePosition, image, ratio, alt, ctaLabel, ctaHref } = Astro.props;
---

<section class:list={['feature-split', `feature-split--${imagePosition}`]}>
  <div class="feature-split__media">
    <Media image={image} ratio={ratio} alt={alt} />
  </div>
  <div class="feature-split__body">
    <h2 class="feature-split__heading">{heading}</h2>
    <p class="feature-split__text">{body}</p>
    {ctaLabel && ctaHref && <Button label={ctaLabel} href={ctaHref} variant="primary" />}
  </div>
</section>

<style>
  .feature-split {
    max-width: 1000px;
    margin: 0 auto;
    padding: var(--sp-6) var(--sp-3);
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--sp-5);
    align-items: center;
  }

  .feature-split--right {
    direction: rtl;
  }

  .feature-split--right > * {
    direction: ltr;
  }

  .feature-split__body {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: var(--sp-2);
  }

  .feature-split__text {
    font-size: var(--font-size-body);
    line-height: 1.7;
    color: var(--color-muted);
    text-align: justify;
  }

  @media (max-width: 767px) {
    .feature-split,
    .feature-split--right {
      grid-template-columns: 1fr;
      direction: ltr;
    }
  }
</style>
